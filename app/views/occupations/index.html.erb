<p id="notice"><%= notice %></p>

<h1>Occupations</h1>

<table>
  <thead>
    <tr>
      <th>Role</th>
      <th>Company</th>
      <th>Location</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @occupations.each do |occupation| %>
      <tr>
        <td><%= occupation.occupation_role %></td>
        <td><%= occupation.occupation_company %></td>
        <td><%= occupation.occupation_location %></td>
        <td><%= link_to 'Show', occupation %></td>
        <td><%= link_to 'Edit', edit_occupation_path(occupation) %></td>
        <td><%= link_to 'Destroy', occupation, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Occupation', new_occupation_path %>
