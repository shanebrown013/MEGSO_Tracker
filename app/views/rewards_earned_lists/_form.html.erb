<%= form_with(model: rewards_earned_list) do |form| %>
  <% if rewards_earned_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rewards_earned_list.errors.count, "error") %> prohibited this rewards_earned_list from being saved:</h2>

      <ul>
        <% rewards_earned_list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :reward_name %>
    <%= form.text_field :reward_name %>
  </div>

  <div class="field">
    <%= form.label :UID %>
    <%= form.number_field :UID %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
